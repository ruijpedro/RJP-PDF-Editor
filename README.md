# RJP PDF Editor V0.6.1

Versão corrigida para GitHub Pages.

## Correção de build
- Removido o ícone `SaveAs` que não existe na versão instalada de `lucide-react`.
- O botão **Guardar como...** usa agora o ícone `Download`, compatível com `lucide-react`.
- Mantém edição de PDF/Word, Guardar, Guardar como e ditado por voz.

## GitHub Pages
O workflow está em `.github/workflows/webapp.yml`.
